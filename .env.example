COMPOSE_PROJECT_NAME=smart-opex

# Exposed host ports
API_PORT=3000
FRONTEND_PORT=5173

# Frontend -> Backend base URL
VITE_API_URL=http://159.13.54.27:3000
VITE_API_PORT=3000

# Backend CORS allowlist (comma-separated, or * for demo only)
FRONTEND_ORIGINS=/usr/bin/python3.27:5173

# OCR runtime - choose based on deployment:
# - Local dev minimal: /usr/bin/python3
# - Full OCR with PaddleOCR (recommended for production): /opt/venv/bin/python
# The Dockerfile builds /opt/venv with all ML dependencies
OCR_PYTHON=/opt/venv/bin/python
# OCR script path is managed dynamically from web UI (uploads/ocr-engine/current.json)
